{
  "name": "lastikli-whatsapp-worker",
  "version": "1.0.0",
  "description": "WhatsApp Web bridge for lastiklicarsaf.tr — sends order/cargo notifications via whatsapp-web.js",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.26.0"
  }
}
